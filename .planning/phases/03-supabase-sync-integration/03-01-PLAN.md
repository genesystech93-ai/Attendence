# Plan 03-01: Configure SQL tables, RLS policies, and triggers on Supabase

This plan outlines the steps to build the database backend schema, Row-Level Security (RLS) policies, and auth-sync triggers on a Supabase PostgreSQL database.

---

## Proposed Changes

### [Backend Schema]

#### [NEW] [schema.sql](file:///c:/Users/Suraj/Desktop/Attendence/backend/schema.sql)
Create a PostgreSQL schema file under a new `backend/` directory, detailing:
- Core tables: `offices`, `users`, `attendance_logs`, `leave_requests`.
- RLS configurations for profile updates, check-ins, and leave sign-offs.
- Plpgsql trigger function to copy users from Supabase Auth to `public.users` instantly.

#### [NEW] [.env.example](file:///c:/Users/Suraj/Desktop/Attendence/desktop/.env.example)
Create a `.env.example` file in the `desktop` and `mobile` directories to define standard environment variables for client connections:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

---

## Step-by-Step Tasks

- [ ] **Task 03-01-01: Write SQL Schema DDL and RLS Policies**
  - Create directory `backend/` in workspace root.
  - Write complete SQL script to `backend/schema.sql` (defining tables, relationships, constraints).
  - Add Plpgsql functions and trigger definitions.
- [ ] **Task 03-01-02: Create Environment Templates**
  - Add `desktop/.env.example` containing Supabase URL and Key placeholders.
  - Add `mobile/.env.example` containing Supabase URL and Key placeholders.

---

## Verification Plan

### Automated Verification
- Verify SQL script syntax using Postgres compiler validation:
  ```bash
  node -c backend/schema.sql
  ```

### Manual Verification
- Review the SQL script in a text editor to ensure all foreign keys link correctly and constraints match active requirements.
