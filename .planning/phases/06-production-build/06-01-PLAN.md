# Plan 06-01: Configure compilation scripts and generate release binaries

This plan details the steps to compile client applications into production-grade Windows and Android executables.

---

## Proposed Changes

### [Desktop Configurations]
#### [MODIFY] [tauri.conf.json](file:///c:/Users/Suraj/Desktop/Attendence/desktop/src-tauri/tauri.conf.json)
- Set bundle configurations, production identifier naming keys, build icons, version tags, and package details.

### [Mobile Configurations]
#### [MODIFY] [app.json](file:///c:/Users/Suraj/Desktop/Attendence/mobile/app.json)
- Set package identifiers (`com.attendy.mobile`), version code variables, and permission checks.

---

## Step-by-Step Tasks

- [ ] **Task 06-01-01: Update Configuration Metadata**
  - Update `desktop/src-tauri/tauri.conf.json` packaging options.
  - Update `mobile/app.json` Android settings.
- [ ] **Task 06-01-02: Compile Build Bundles**
  - Build Tauri desktop: `npm run tauri build`.
  - Export Expo bundle files: `npx expo export`.

---

## Verification Plan

### Automated Verification
- Verify build compiling processes:
  ```bash
  cd desktop && npm run build
  ```

### Manual Verification
- Launch the Windows desktop installer bundle generated by Tauri. Verify the app installs and opens.
