# Plan 04-01: Implement WorldTimeAPI time queries and SQL row insertion constraints

This plan covers integrating client-side network time queries and database-level time integrity constraints.

---

## Proposed Changes

### [Backend]
#### [MODIFY] [schema.sql](file:///c:/Users/Suraj/Desktop/Attendence/backend/schema.sql)
- Add database check constraints ensuring `check_in` and `check_out` values match database transaction timestamps within 10 seconds.

### [Client Desktop & Mobile]
#### [MODIFY] [dataService.ts](file:///c:/Users/Suraj/Desktop/Attendence/desktop/src/services/dataService.ts)
- Modify `checkIn` and `checkOut` functions to perform an HTTP request to `http://worldtimeapi.org/api/timezone/Etc/UTC` first to get network time. Use this timestamp when passing parameters.

#### [MODIFY] [dataService.ts](file:///c:/Users/Suraj/Desktop/Attendence/mobile/src/services/dataService.ts)
- Implement matching secure timezone network queries in mobile.

---

## Step-by-Step Tasks

- [x] **Task 04-01-01: Update Backend DDL constraints**
  - Add check constraints to `public.attendance_logs` table in `schema.sql`.
- [x] **Task 04-01-02: Implement Client Network Time Queries**
  - Update `desktop` data service to fetch network time.
  - Update `mobile` data service to fetch network time.
  - Implement fallback handling to prompt alerts if device clock is out-of-sync.
- [x] **Task 04-01-03: Run verification builds**
  - Run compile build check for Tauri desktop and Expo mobile.

---

## Verification Plan

### Automated Verification
- Verify code compiles:
  ```bash
  cd desktop && npm run build
  ```

### Manual Verification
- Artificially change your computer system time. Click Punch In. Verify that the check-in fails with a warning or logs the correct time from the API.
