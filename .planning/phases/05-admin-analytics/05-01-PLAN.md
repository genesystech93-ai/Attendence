# Plan 05-01: Build stats dashboards and CSV download tools

This plan covers implementing aggregated SQL reports and client-side CSV downloads for administrator views.

---

## Proposed Changes

### [Backend]
#### [MODIFY] [schema.sql](file:///c:/Users/Suraj/Desktop/Attendence/backend/schema.sql)
- Append the definition for PostgreSQL RPC `get_attendance_summary` to return aggregated working statistics.

### [Desktop]
#### [MODIFY] [dataService.ts](file:///c:/Users/Suraj/Desktop/Attendence/desktop/src/services/dataService.ts)
- Add database helper to query the custom RPC and format rows.
#### [MODIFY] [App.tsx](file:///c:/Users/Suraj/Desktop/Attendence/desktop/src/App.tsx)
- Insert a "Download CSV Report" button inside the admin dashboard.
- Create a visualization area presenting total monthly hours and late counts.

---

## Step-by-Step Tasks

- [ ] **Task 05-01-01: Write SQL RPC Aggregates**
  - Add `get_attendance_summary` procedure to `backend/schema.sql`.
- [ ] **Task 05-01-02: Implement Client CSV Export Logic**
  - Code `exportToCSV` utility in the desktop application.
  - Bind the UI button action to trigger the downloader.
- [ ] **Task 05-01-03: Run verification builds**
  - Run compile build check inside `desktop/` directory.

---

## Verification Plan

### Automated Verification
- Verify build compiling:
  ```bash
  cd desktop && npm run build
  ```

### Manual Verification
- In the Admin Panel, click "Download CSV Report." Verify that a CSV file downloads, containing accurate hours and check-in statuses.
